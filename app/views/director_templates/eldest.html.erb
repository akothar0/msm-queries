<h1>Eldest director</h1>
<% the_director = Director.where.not({ :dob => nil }).order({ :dob => :asc })[0] %>
<p>The eldest director in our dataset right now is <a href="/directors/<%= the_director.id%>"><%= the_director.name%></a>, born on <%= the_director.dob.strftime("%B %d, %Y")%>.</p>
